<resources>
    <!-- 微信6.0actionbar主题. -->
    <style name="AppBaseTheme" parent="android:Theme.Holo.Light.DarkActionBar">
        <item name="android:actionOverflowButtonStyle">@style/weixinActionOverflowButtonstyle</item>
    </style>

    <style name="weixinActionOverflowButtonstyle">
        <item name="android:src">@drawable/actionbar_add_icon</item>
    </style>

    <!-- 首页预加载主题. -->
    <style name="MainWindowTheme" parent="android:Theme.Holo.Light.NoActionBar">
        <item name="android:background">@drawable/layerlist_logo</item>
    </style>

    <!-- Base application theme. -->
    <!-- appcompat vs holo 为何冲突报错 -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>

    <style name="anim_popup_dir">
        <item name="android:windowEnterAnimation">@anim/slide_in</item>
        <item name="android:windowExitAnimation">@anim/slide_out</item>
    </style>

    <!-- 文字白色阴影style-->
    <style name="text_white_shadow">
        <item name="android:shadowColor">@color/white</item>
        <item name="android:shadowDy">-2</item>
        <item name="android:shadowRadius">1</item>
    </style>
    <!-- 文字黑色阴影style-->
    <style name="text_black_shadow">
        <item name="android:shadowColor">@color/black</item>
        <item name="android:shadowDy">-2</item>
        <item name="android:shadowRadius">1</item>
    </style>
    <!-- guess music 自定义对话框的主题-->
    <style name="GuessMusic.Theme.Transparent" parent="Base.Theme.AppCompat.Light.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:layout_marginLeft">2dp</item>
        <item name="android:layout_marginRight">2dp</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="function_spinner_style">
        <item name="android:background">@drawable/function_selector_spinner</item>
    </style>

</resources>
